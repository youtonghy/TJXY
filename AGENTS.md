# TJXY Agent 工作规范

本文件适用于整个仓库。基础协作原则参考 [OpenAI Model guidance](https://developers.openai.com/api/docs/guides/latest-model#prompting-best-practices)，提交前检查是本项目的强制要求。

## 基础协作原则

- 根据用户当前要求及已有上下文确定目标、范围和验收条件。行动请求应落实为完成的改动，不能只给计划或承诺。
- 对已获授权的调查、可逆修改和验证主动推进，不重复请求确认。只有缺失信息会实质改变结果且无法合理推断，或操作确实需要额外授权时，才提出必要的问题；先完成不依赖回答的工作。
- 用户补充要求时保留已完成成果并调整后续工作；回答中途问题后继续原任务，除非用户明确取消或替换目标。
- 遵守上级指令；用户明确要求优先于技能中的建议。若技能规则导致暂停或未完成任务，说明具体 `SKILL.md` 路径、相关原文及适用原因，不能把自行推断的限制说成强制要求。
- 默认用简洁中文沟通，先说结果，再说明必要的依据和限制。耗时工作及时更新有意义的进展；最终说明改动、验证结果和未完成事项。
- 仅在用户要求或适用指令明确要求时委派子代理；委派应有明确边界，主代理负责整合和验证结果。

## 仓库与改动原则

- `admin/`：React、TypeScript、Vite 前端；脚本以 `admin/package.json` 为准。
- `crates/`、`tjxy-tui/`：Rust 后端及终端界面；工作区成员以根目录 `Cargo.toml` 为准。
- `.github/workflows/release.yml`：发布流程及前端、Rust 验证命令的依据。
- 修改前检查工作区状态，阅读相关实现与测试，保留用户和其他任务的改动。不要覆盖、回退或提交无关变更。
- 保持改动聚焦，沿用现有架构和代码风格；不顺带大规模重构或升级依赖。
- 行为修复应增加能复现问题的回归测试。API 字段、序列化结果或路由响应变化时，检查对应契约测试及 golden 预期；确认预期行为后再更新，不能机械接受快照。
- 不通过删除断言、忽略失败用例或放宽检查来制造通过结果。不得在代码、测试输出或提交中泄露密钥、令牌等敏感信息。

## 创建 commit 前必须自动执行的检查

每次创建 commit（包括 amend 和仅文档改动）前，Agent 必须主动执行以下完整检查，无需等待用户再次提醒。只有所有命令正常结束且退出码为 `0`，才允许创建 commit。该要求也适用于本文件自身的提交。

### 环境准备

- 从仓库根目录执行命令，依赖应与锁文件一致；缺失或不一致时运行 `npm --prefix admin ci`。不要为绕过安装失败擅自更新锁文件。
- Node.js 与 CI 保持一致，目前为 `22.22.3`；前端声明的最低版本为 `22.12.0`。
- Rust 与 CI 的 `RUST_VERSION` 保持一致，目前为 `1.88.0`，并安装 `clippy`、`rustfmt`。不得仅用本机更新版本的工具链通过结果代替 CI 版本验证。
- CI 工具链或检查脚本变化时，同步更新此处的版本和命令。若构建需要 HeroUI Pro 配置，按现有项目流程配置，不能把凭据写入仓库。

### 前端完整检查

```sh
npm --prefix admin run typecheck
npm --prefix admin run lint
npm --prefix admin test -- --run
npm --prefix admin run build
```

### Rust 编译、全工作区测试与静态检查

Rust 测试通过 Cargo 调用 rustc 编译并运行，不能只执行 `rustc --version`、`cargo check` 或个别测试代替完整验证。

```sh
cargo +1.88.0 test --workspace --locked -- --test-threads=2
cargo +1.88.0 clippy --workspace --all-targets --locked -- -D warnings
cargo +1.88.0 fmt --all -- --check
```

### 检查与提交的约束

- 开发中可以先跑相关测试加快反馈，但提交前必须完成上述两组完整检查，不能因“只改前端”“只改 Rust”或“只改文档”省略另一组。
- 不使用测试名称筛选或跳过选项代替全量检查；涉及浏览器交互等未被上述检查覆盖的行为时，补充相应验证。
- 任一检查失败，先诊断、修复，再重新完成提交前检查。失败、超时、中断、依赖缺失、权限或环境阻塞都不是通过，均不得创建 commit；明确报告阻塞原因和未完成的检查。
- 等待所有检查结束并读取结果，不能在后台检查仍运行时提交，也不能将启动成功当成验证成功。
- 检查结果必须对应即将提交的最终文件内容。检查后再次修改内容，必须重新完成上述检查；提交前审阅暂存区，避免部分暂存导致被测试内容与提交内容不一致。
- 创建 commit 前还须运行 `git diff --check` 和 `git diff --cached --check`，确认无空白错误并核对提交范围。
- 不伪造测试结果，不把历史通过记录当成本次结果。交付时简要列出实际执行的检查及结果；若创建了 commit，提供提交标识。
- 开发期间按改动风险选择验证，不为低影响改动编写只复述实现的测试。完整提交检查通过后，若内容未再变化且没有新疑点，无需重复运行。

本文件规定 Agent 的执行行为；其本身不会安装 Git hook。手动运行 Git 不会因本文件存在而自动触发测试。
