# 元数据任务重试与本地索引恢复

## 运行行为

- 首次执行失败后，持久工作任务最多自动重试 5 次（合计最多 6 次执行）。第 5 次重试仍失败时，任务转为 `Failed`，记录达到上限及跳过原因，释放活动任务键；工作线程继续处理其他任务。
- 升级前已经超过此上限的 Pending 任务，在领取阶段直接失败，不再执行。历史 Completed/Failed 记录不改写。
- 索引校验、目录输入及扫描子任务尚未完成时，采用独立的依赖等待路径，不消耗执行重试次数。等待延迟随任务已开始的时间指数增长，最大 5 分钟；元数据索引等待带有抖动。
- 元数据执行错误采用带抖动的指数退避。确定性的全量扫描依赖冲突直接失败，避免反复提交相同冲突。

## 本地文件系统

运行中创建、添加路径和启动加载均先调用索引准备逻辑，再注册读取后端及存储工作线程。索引需要重建时，状态和校验任务在同一数据库事务内持久化。身份一致的历史 NULL 配置可以初始化；身份变化的配置仍隔离读取，校验发布后才能恢复。

数据库诊断区分未初始化、Ready、Rebuilding 和 Failed。校验任务失败（包括达到重试上限）或取消时，重建中的索引同时标记失败。管理员重新提交校验时，Failed 索引与新校验任务在同一事务内进入重建流程。

显式注入、没有持久 filesystem 配置的后端保留既有行为；真实创建接口的回归测试使用有持久配置的媒体库。

## 任务合并

发现、点击加载、来源索引和完整扫描的元数据任务使用一致的根目录归属。旧任务缺少归属时，只在目录归属唯一、输入版本与依赖一致、任务仍为 Pending 的情况下补齐并复用。Running 任务的快照依赖不改写；跨根或输入版本冲突不合并。

管理员手动元数据任务沿用媒体库的元数据来源与本地导入策略。冲突日志包含已有与请求任务标识、根目录、输入版本和依赖标识。

## 管理页面与恢复

任务接口增加可选字段 `LastError`、`WaitingReason`、`NextAttemptAt`、`ValidationJobId`、`ValidationStatus`。页面显示安全错误摘要、等待原因、下次尝试时间及关联校验任务状态。外部后端原始错误不直接返回浏览器，以免泄露连接凭据或签名链接。

对于索引校验失败：先修复存储问题并在任务页面重新提交存储校验，待其成功后，对失败的元数据任务使用“恢复后重新排队”。过期快照需要提交新任务，不复活旧租约。

## 升级与验证

本修复复用现有字段，不增加数据库迁移。部署新版并重启一次后，已有活动本地账户会经过身份核对和初始化。代码回滚不会撤销已完成的状态初始化或恢复自动重试预算；数据库无需回滚结构。

回归覆盖：真实 HTTP 创建媒体库并完整扫描后读取 NFO 和图片（不重启）、持久 NULL 初始化、物理身份变化隔离、校验失败后重新排队、兼容历史任务复用、运行中任务保护、跨根/版本冲突、重试边界及前端错误展示。

本地测试不等于线上恢复验证；本次未部署、重启线上服务或修改线上数据库。

## 本次验证结果

- 后端单元测试：数据库 14 项、服务端 61 项通过。
- 契约测试：任务队列 23 项、目录发现 10 项、完整扫描 18 项、元数据 20 项、来源索引 1 项通过。
- HTTP 端到端：创建媒体库、完整扫描、NFO 简介和海报读取通过。
- 前端任务页面和接口 26 项测试通过；TypeScript 类型检查、ESLint 和生产构建通过。生产构建保留既有大体积资源分块提示。
- 对 server、db、application、api、storage 执行所有目标的 Clippy 检查，使用 `-D warnings`，通过。
- `git diff --check` 通过。

以上为本机 SQLite 测试，未运行 PostgreSQL/MySQL 环境矩阵，也未执行整个工作区的全部测试。
