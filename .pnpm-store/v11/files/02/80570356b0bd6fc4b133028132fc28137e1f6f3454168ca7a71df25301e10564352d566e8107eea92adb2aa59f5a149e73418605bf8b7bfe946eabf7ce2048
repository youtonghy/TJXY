import { ColorScheme, Scale, Theme } from './types';
export declare function useColorScheme(theme: Theme, defaultColorScheme: ColorScheme): ColorScheme;
export declare function useScale(theme: Theme): Scale;
